#!/usr/bin/env python3

from gedelearn get_model_sequential, compile, train, show

if __name__ == '__main__':
    model = m.get_model_sequential()
    compile.compile(model)
    train.train(model)
    view.show(model)
